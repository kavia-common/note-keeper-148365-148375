# note-keeper-148365-148375

This workspace hosts the Notes Frontend (React).

How to run:
1) cd notes_frontend
2) npm install
3) npm start
Frontend will run on http://localhost:3000 and expects backend at http://localhost:3001 (configurable via REACT_APP_API_BASE_URL).